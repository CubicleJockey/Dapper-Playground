This is a playground to try out and test Dapper as a Micro-ORM.

To get started you will need to load Northwind Database. All the current methods of examples do not modify the database other that in one case.
In this case the update is to the already present value just to check if update completes successful.

Northwind Database Download: 
https://northwinddatabase.codeplex.com/
